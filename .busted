-- Focused unit/failure tests. Real application workflows use just test-e2e.
return {
  _all = {
    lua = 'nlua',
    helper = 'tests/init.lua',
    pattern = '_spec',
  },
  default = { ROOT = { 'tests/core' } },
  core = { ROOT = { 'tests/core' } },
}
