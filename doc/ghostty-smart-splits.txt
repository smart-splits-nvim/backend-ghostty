*ghostty-smart-splits.txt*  Neovim-first Ghostty navigation on macOS

==============================================================================
CONTENTS                                                *ghostty-smart-splits*

  1. Requirements                          |ghostty-smart-splits-requirements|
  2. Setup                                 |ghostty-smart-splits-setup|
  3. API                                   |ghostty-smart-splits-api|
  4. Limitations                           |ghostty-smart-splits-limitations|

==============================================================================
1. REQUIREMENTS                           *ghostty-smart-splits-requirements*

Neovim 0.11+, smart-splits.nvim, Ghostty 1.3+ on macOS, and macOS Automation
permission. Run Neovim locally inside Ghostty. Configure Ghostty's nvim key
table before starting Neovim; the ready-to-copy configuration is in
examples/ghostty.conf.

==============================================================================
2. SETUP                                         *ghostty-smart-splits-setup*

SMART-SPLITS V2

Configure smart-splits normally, then call setup() once at startup with the
Neovim pane focused:
>
  require('smart-splits').setup({}) -- Existing options.
  require('ghostty-smart-splits').setup({})
<
The v2 setup preserves existing smart-splits options and sets
`multiplexer_integration = 'ghostty'` and `at_edge = 'stop'`. It returns false
when the session is unsupported; terminal attachment and key-table activation
continue asynchronously after setup.

SMART-SPLITS V3 (EXPERIMENTAL)

Use smart-splits.nvim's `v3` Git ref and select the backend:
>
  require('smart-splits').setup({
    mux = { backend = 'smart-splits-backend-ghostty' },
    move = { at_edge = 'stop' },
  })
<
Do not call the v2 root module's setup() with v3. Core activates the selected
backend during setup; start Neovim with its Ghostty pane focused. The v3
protocol may change before release.

MAPPINGS

Neither plugin creates mappings automatically. After your plugin setup (after
require('lazy').setup(...) for lazy.nvim), add these Normal-mode mappings.
They work with v2 and v3 and match examples/ghostty.conf:
>
  local splits = require('smart-splits')
  vim.keymap.set('n', '<C-h>', splits.move_cursor_left)
  vim.keymap.set('n', '<C-j>', splits.move_cursor_down)
  vim.keymap.set('n', '<C-k>', splits.move_cursor_up)
  vim.keymap.set('n', '<C-l>', splits.move_cursor_right)
  vim.keymap.set('n', '<M-h>', splits.resize_left)
  vim.keymap.set('n', '<M-j>', splits.resize_down)
  vim.keymap.set('n', '<M-k>', splits.resize_up)
  vim.keymap.set('n', '<M-l>', splits.resize_right)
<
<M-...> is the Mac Option/Alt key. Configure Ghostty's matching key table too.

V3 EDGE BEHAVIOR                             *ghostty-smart-splits-at-edge*

Configure move.at_edge in smart-splits, not in the backend options. Movement
first tries a Neovim window, then a neighboring Ghostty pane. Only when neither
exists in the requested direction does the edge setting apply:

  stop     Stay in the current Neovim window.
  wrap     Wrap to the opposite edge of the Neovim layout in this Ghostty
           pane. With one Neovim window, stay there. No Ghostty pane wrapping.
  split    Create and focus a Ghostty pane in that direction. If Ghostty
           cannot create it, core falls back to creating a Neovim split.

All three modes navigate to existing Ghostty neighbors. In particular, stop
does not disable crossing the Neovim/Ghostty boundary. Custom move.at_edge
functions are handled by smart-splits when the backend cannot move.

ZOOM AND FULLSCREEN                         *ghostty-smart-splits-zoom*

The backend does not detect zoom/fullscreen or suppress navigation in those
states. Neovim windows still take priority; at an editor edge, Ghostty handles
the usual goto_split action. There is no disable_nav_when_zoomed option.
On Ghostty 1.3.1, moving to a neighboring pane leaves split zoom. Window
fullscreen allows navigation inside Neovim and across Ghostty panes.

CONFIGURATION

`key_table` is the name of the Ghostty key table used while Neovim is active.
It defaults to `nvim` and must be a non-empty string. Changing it to a
different name while it is claimed is an error; release it first.

`transport` selects how actions and pane lookups reach Ghostty. It defaults to
`persistent`, which keeps one osascript process running and falls back to
ephemeral when that process cannot answer. `ephemeral` starts osascript for
every request.

`bridge` is a deprecated boolean alias for `transport`: true selects
`persistent` and false selects `ephemeral`. It still works and warns once.

Use the v2 root module or the v3 backend module, depending on the integration:
>
  -- v2
  -- transport = 'ephemeral' starts osascript for every request instead.
  require('ghostty-smart-splits').setup({ key_table = 'editor', transport = 'persistent' })

  -- v3: configure the backend before smart-splits setup.
  require('smart-splits-backend-ghostty').setup({
    key_table = 'editor',
    transport = 'persistent', -- Or 'ephemeral' to start osascript per request.
  })
<
Configuration does not run AppleScript, start the persistent process, attach
to Ghostty, or register autocommands. Switching to ephemeral stops a running
persistent process immediately. Switching to persistent takes effect on the
next attachment or action. In v2, the root setup continues with attachment and
activation; in v3, core activates the selected backend.

setup() merges over the current options: a call that names one option leaves
the rest alone, so the transport can be switched at runtime without repeating
key_table. An unknown option name is an error rather than a silent no-op.
Call config.reset() to restore every default.

In local measurements, actions took about 15 ms with persistent versus about
100 ms with ephemeral; results vary by machine.

The persistent transport keeps scripts/ghostty.js running in one osascript
process, so the JavaScript engine and the Ghostty process lookup are set up
once rather than for every request. There is nothing to build. Each Neovim
instance owns one persistent process and stops it on exit. The initial
terminal lookup always uses ephemeral osascript, asynchronously, before the
persistent process is running.

Earlier versions started osascript for every request unless a compiled bridge
was built and enabled. `make bridge` from those versions is deprecated: it no
longer builds anything and only prints a notice, so remove it from plugin
manager build steps.

smart-splits v2 brackets each pane move with a terminal lookup, so one move is
three Ghostty requests. All three use the persistent process when it is
enabled.

==============================================================================
3. API                                             *ghostty-smart-splits-api*

require('ghostty-smart-splits').claim_keys()
    Push the configured Ghostty key table if it is not already claimed.

require('ghostty-smart-splits').release_keys()
    Pop the table claimed by this instance. Keys are also released on
    VimSuspend and VimLeavePre, and claimed again on VimEnter and VimResume.

require('ghostty-smart-splits.config').reset()
    Restore every option to its default. setup() merges rather than
    replaces, so this is the only way back to the defaults.

:checkhealth ghostty-smart-splits
    Check local prerequisites, the selected transport, and whether the
    persistent process is running, without starting it. With v3,
    :checkhealth smart-splits also includes backend diagnostics.

==============================================================================
4. LIMITATIONS                            *ghostty-smart-splits-limitations*

macOS only. Action AppleScript calls are synchronous and time out after one
second.
The initial Ghostty terminal comes from the focused pane and the lookup is
asynchronous. Later actions keep using that terminal instead of following
focus changes.
A failed lookup is retried when Neovim next regains focus, which is when the
Automation prompt has been answered and is also proof the focused pane is
still ours. After five failures the plugin stops trying and warns once.

Do not stack another Ghostty key table above this one while Neovim is active.
If a crash or config reload leaves stale state, the Ghostty action
deactivate_all_key_tables can recover it, but clears every table.

Native performable:goto_split bindings can give Ghostty priority. This plugin
is for Neovim-first navigation.

 vim:tw=78:ts=8:noet:ft=help:norl:
